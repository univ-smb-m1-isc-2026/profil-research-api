# profil-research-api
From *demo* folder in a terminal to run the backend enter: 
```
./mvnw spring-boot:run
```


Test API :
```
curl http://localhost:8080/hello
```

sécurité sur les route csrf -> à ajouter les routes get